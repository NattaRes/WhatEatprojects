<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.whateatprojects">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhatEatprojects">
        <activity
            android:name=".resturList"
            android:exported="false" />
        <activity
            android:name=".menufood"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".menufood" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".listdetail"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".listres" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".foodlist"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".Listfood" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".Home"
            android:exported="false"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.WhatEatprojects.NoActionBar" />
        <activity
            android:name=".Login"
            android:exported="false" />
        <activity
            android:name=".Register"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>